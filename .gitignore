# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
# ============ SECRETS — jamais sur GitHub ============
# Le .env réel (dev local) et toutes ses variantes restent locaux.
# En production (Vercel/Netlify/Render...), renseigner REACT_APP_API_URL
# directement dans le dashboard — aucun fichier .env n'est envoyé.
.env
.env.*
!.env.example

npm-debug.log*
yarn-debug.log*
yarn-error.log*

